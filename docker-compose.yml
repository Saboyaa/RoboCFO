services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    # Port 8000 not exposed to host — nginx proxies internally via Docker network
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
